# i3 config file (v4)
# Please see https://i3wm.org/docs/userguide.html for a complete reference!
#
# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).

# As many keybinds as I can fit up here context-free. Some other i3 elements
# below will need to set keybinds later (done as-close-to-element as possible)

# i3 font for window titles. Will also be used by the bar unless overridden in
# the bar {} block describing i3bar's specification (below).
font pango:monospace 12

# Alt
set $mod Mod1
# Super
set $altmod Mod4

# Define names for default workspaces for which we configure key bindings later
# on. We use variables to avoid repeating the names in multiple places.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# Workspace assignments to monitors
workspace $ws1 output DisplayPort-1
workspace $ws2 output DisplayPort-0
workspace $ws3 output HDMI-A-0
workspace $ws4 output DisplayPort-0
workspace $ws5 output HDMI-A-0

# i3 can successfully force SOME applications to properly open on a default workspace
# assignment, with caveats below:
# NOTE: Have to use "number" prefix as I will rename the workspaces dynamically!
# Failure to do so will launch to workspace named literally "#", then rename it
# (so you'd have multiple workspaces of the same number and break numeric-based
#  workspace-swapping, a la "3: Discord" and "3: Firefox" simultaneously existing,
#  where MOD+3 can ONLY select the first 3-valued instance)
# NOTE: Names have to be fully qualified, so use the settings.json / automanager.py
#  to flexibly assign classes (ie: steam_app_*) to a given workspace. There's
#  still a bug where it can get overzealous with applications that preload or
#  relaunch themselves (where a previous, unrelated window may get dragged over
#  there too during renaming etc). Firefox Picture-in-Picture will ALWAYS assign
#  once (to the default workspace), so you have to swap over to its default
#  workspace to see PiP if the parent-window has been moved to a different
#  workspace. However, assigning firefox via settings.json / automanager.py
#  would YANK the entire browser window back to the default workspace, so I
#  think this is a better solution unless/until I can fix that bug with
#  automanager's behavior
assign [class="^obsidian$"] number 1
assign [class="^firefox_firefox$"] number 3
assign [class="^discord$"] number 3
assign [class="^obs$"] number 5
assign [class="^kdenlive$"] number 5
assign [class="^steam$"] number 5
assign [class="^Lutris$"] number 5

# Reload the configuration file
bindsym $mod+Shift+c reload
# Restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# Exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exit
#bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

# Enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle
# Kill focused window
bindsym $mod+Shift+q kill
# dmenu_run replacement (a program launcher)
bindsym $mod+d exec --no-startup-id python3 ${HOME}/.config/i3/special_dmenu_handler.py
# Start a terminal
bindsym $mod+Return exec i3-sensible-terminal

# Screenshots via maim alias with xclip
bindsym $altmod+s exec --no-startup-id ${HOME}/Tools/screenshot.sh
bindsym $altmod+Shift+s exec --no-startup-id ${HOME}/Tools/screenshot.sh --select

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod
# Move tiling windows via drag & drop by left-clicking into the title bar,
# or left-clicking anywhere into the window while holding the floating modifier.
tiling_drag modifier titlebar
# Do NOT move focus wherever the mouse is
focus_follows_mouse no
# Focus new windows
focus_on_window_activation focus

# Change focus
bindsym $mod+h focus left
bindsym $mod+Left focus left
bindsym $mod+j focus down
bindsym $mod+Down focus down
bindsym $mod+k focus up
bindsym $mod+Up focus up
bindsym $mod+l focus right
bindsym $mod+Right focus right

# Move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+l move right
bindsym $mod+Shift+Right move right

# Switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# Move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1; workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2; workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3; workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4; workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5; workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6; workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7; workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8; workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9; workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10; workspace number $ws10


# Scripts that i3 spawns

# Setup monitors correctly in i3
exec_always ${HOME}/.screenlayout/TripleMonitorLayout.sh

# Start XDG autostart .desktop files using dex. See also
# https://wiki.archlinux.org/index.php/XDG_Autostart
exec --no-startup-id dex --autostart --environment i3

# My python script for renaming workspaces based on their content
exec_always --no-startup-id python3 ${HOME}/.config/i3/automanager.py

# xss-lock grabs a logind suspend inhibit lock and will use i3lockwall to lock
# the screen before suspend. Use loginctl lock-session to lock your screen.
# Locking exit, preserves windows
set $i3lockwall "${HOME}/.config/i3/./sleeplock.sh"
exec --no-startup-id xset s off
exec --no-startup-id xset -dpms
exec --no-startup-id xss-lock --transfer-sleep-lock -- $i3lockwall
# Lock screen shortcut
bindsym $mod+Shift+t exec --no-startup-id $i3lockwall


# Status bar and related components

# Use amixer to adjust volume.
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id amixer -D pulse sset Master 5%+ && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id amixer -D pulse sset Master 5%- && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id amixer -D pulse set Master 1+ toggle  && $refresh_i3status
#bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

# NetworkManager is the most popular way to manage wireless networks on Linux,
# and nm-applet is a desktop environment-independent system tray GUI for it.
exec --no-startup-id nm-applet

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
        #status_command i3status --config ~/.config/i3/i3status.conf
        status_command ~/.config/i3/i3status_cmd.sh
        #colors {
        #    background $black2
        #    statusline $white2
        #    separator $white2
        #    # ZONE             Border      Background  Text
        #    focused_workspace  $background $background $red1
        #    active_workspace   $black2     $black1     $white2
        #    inactive_workspace $black1     $black2     $foreground
        #    urgent_workspace   $red1       $red1       $white2
        #    binding_mode       $background $red2       $white2
        #}
}

# Setup desktop wallpaper
#exec_always feh --bg-fill <IMAGE>




# Disabled portions of default i3 config that maybe I would want to return to...

# Split in vertical orientation
#bindsym $mod+v split v
# Split in horizontal orientation
#bindsym $mod+semicolon split h
# Change container layout (stacked, tabbed, toggle split)
#bindsym $mod+s layout stacking
#bindsym $mod+w layout tabbed
#bindsym $mod+e layout toggle split
# Change focus between tiling / floating windows
#bindsym $mod+space focus mode_toggle
# Focus the parent container
#bindsym $mod+a focus parent
# Focus the child container
#bindsym $mod+d focus child
# Toggle tiling / floating
#bindsym $mod+Shift+space floating toggle

# Resize window (you can also use the mouse for that)
#mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        #bindsym h resize shrink width 10 px or 10 ppt
        #bindsym j resize grow height 10 px or 10 ppt
        #bindsym k resize shrink height 10 px or 10 ppt
        #bindsym l resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        #bindsym Left resize shrink width 10 px or 10 ppt
        #bindsym Down resize grow height 10 px or 10 ppt
        #bindsym Up resize shrink height 10 px or 10 ppt
        #bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        #bindsym Return mode "default"
        #bindsym Escape mode "default"
        #bindsym $mod+r mode "default"
#}
#bindsym $mod+r mode "resize"

